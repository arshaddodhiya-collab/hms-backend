spring.application.name=HospitalManagementSystem

# MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/hospital_db?createDatabaseIfNotExist=true
spring.datasource.username=Arahsad
spring.datasource.password=Arashad@6139
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Configuration
# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Configuration
# 24 hours in milliseconds
jwt.expiration=86400000
# IMPORTANT: Use a strong secret in production (at least 256 bits)
jwt.secret=changeme_to_a_secure_secret_key_at_least_32_chars_long

